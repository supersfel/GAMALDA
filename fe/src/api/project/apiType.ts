export interface testType {
  test: string;
}

//getProjectInfo Type
export interface getProjectInfoType {
  name: string;
  image: string;
}

export interface getProjectInfoProps {
  projectId: string;
}

export interface getNaverQuery {
  code: string;
}